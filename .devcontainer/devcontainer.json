{
  "name": "Journaling Vault",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git-crypt && echo \"$GPG_PRIVATE_KEY\" | gpg --import",
  "customizations": {
    "vscode": {
      "extensions": [
        "yzane.markdown-pdf"
      ]
    }
  }
}


